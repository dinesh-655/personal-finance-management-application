# sqlite3 is part of Python's standard library and typically doesn't need to be installed separately.
# However, if you want to explicitly list it or are working in environments where it's not included:
sqlite3
